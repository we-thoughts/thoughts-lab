
import SystemApp from './app'

let { app, login, verify, profile, custom_tab_bar, changelog } = SystemApp

export default { app, login, verify, profile, custom_tab_bar, changelog }

export { app, login, verify, profile, custom_tab_bar, changelog }
